# Creating a tuple using ()
t = (1,2,5,6,7)
print(t[3])

# We can't access the tuple i.e tuple is immutable
# t[2]=45
# print(t)   # It give error because we can't cahnge the element of list

# t1 = ()  # empty tuple
# t1 = (1) #Wrong way to declare a Tuple with Single element
t1 =(1,)
print(t1,type(t1))

